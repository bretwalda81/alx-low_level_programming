#include "main.h"

/**
* _islower - checks for lowercase character
* @x: the integer that is the functions parameter
* Return: 0 if sucessful
*/
int _islower(int x)
{
if (x >= 'a' && x <= 'z')
{
return (1);
}
else
return (0);
}
