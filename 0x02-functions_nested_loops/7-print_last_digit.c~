#include "main.h"

/**
* print_last_digit - prints the alst digit of a number
* @n: the number that is the parameter of the function
* Return: the value of the last digit
*/
int print_last_digit(int r)
{
int x;

if (r < 0)
{
r = -r;
}
x = r % 10;

if (x < 0)
{
x = -x;
}
_putchar(x + '0');
return (x);
}
