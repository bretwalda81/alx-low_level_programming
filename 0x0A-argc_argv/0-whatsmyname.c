#include <stdio.h>

/**
* main - prints its own name
* @argc: number of command line arguments
* @argv: arguments sstring
* Return: always 0
*/
int main(int argc, char *argv[])
{
printf("%s\n", argv[argc * 0]);
return (0);
}
