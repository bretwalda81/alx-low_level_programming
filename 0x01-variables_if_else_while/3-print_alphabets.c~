#include <stdio.h>

/**
*main- printa the entire alphabet
*in lower case ending with newline
*Return: Always 0 (Success)
*/
int main(void)
{
char x = 'a';

while (x <= 'Z')
{
putchar(x);
x = x + 1;
}
putchar(10);
return (0);
}
