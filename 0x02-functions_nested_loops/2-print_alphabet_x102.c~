#include "main.h"

/**
* print_alphabet_x10 - function
* to print the alphabets 10 times in lower case
*
* Return: Always 0 (Success0
*/
void print_alphabet_x10(void)
{
char x, y;
for (x = 0; x <= 9; x++)
{
for (y = 'a'; y <= 'z'; y++)
{
_putchar(c);
}
_putchar('\n')
}
}
