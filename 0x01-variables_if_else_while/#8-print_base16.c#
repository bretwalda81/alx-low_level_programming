xn#include <stdio.h>

/**
*main - prints all base 16 numbers in lower case
*Return: Always 0 (Success)
*/
int main(void)
{
  int x, y;

for (x = 0; x < 10; x++)
{
putchar(x);
}
for (y = 'a'; y <= 'f'; y++)
{
putchar(y);
}
putchar(10);
return (0);
}
